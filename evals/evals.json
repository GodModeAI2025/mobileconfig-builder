{
  "skill_name": "mobileconfig-builder",
  "evals": [
    {
      "id": 1,
      "prompt": "Bau mir bitte ein WLAN-Profil für unsere Macs. SSID ist 'GuestNet', WPA2, Passwort 'supersecret123', soll automatisch joinen, nicht versteckt. Identifier com.example.wifi.guest.",
      "expected_output": "Eine valide .mobileconfig mit einem PayloadContent-Eintrag vom Typ com.apple.wifi.managed, korrekten SSID/EncryptionType/Password/AutoJoin-Werten, deterministischen UUIDs, Top-Level PayloadType=Configuration und PayloadVersion=1. Strikte Schema-Validierung muss durchgehen.",
      "files": ["assets/examples/wifi_guest.json"],
      "expectations": [
        "Output is a valid Apple plist (XML, parseable with plistlib)",
        "Top-Level PayloadType is exactly 'Configuration'",
        "Top-Level PayloadVersion is exactly 1",
        "Top-Level PayloadIdentifier matches the input identifier",
        "Top-Level PayloadUUID is a valid UUID v4/v5 string",
        "PayloadContent contains exactly one inner payload",
        "Inner PayloadType is 'com.apple.wifi.managed'",
        "Inner SSID_STR equals 'GuestNet'",
        "Inner EncryptionType is one of the schema's allowed values (WEP/WPA/WPA2/WPA3/Any/None)",
        "Inner AutoJoin is a real boolean (not the string 'true')",
        "Inner Password is preserved exactly",
        "Inner payload has its own PayloadUUID and PayloadIdentifier auto-filled",
        "Strict-mode schema validation passes with zero errors"
      ]
    },
    {
      "id": 2,
      "prompt": "Erstelle ein Profil das auf einem Mac alle Apple-Intelligence-Funktionen abschaltet — Writing Tools, Image Playground, Genmoji, Mail-Zusammenfassungen, externe KI-Integrationen wie ChatGPT, Apple Intelligence Reports, alles. Identifier com.zimmermann.disable-apple-intelligence.",
      "expected_output": "Eine valide .mobileconfig mit einem com.apple.applicationaccess Payload, der mindestens diese Boolean-Keys enthält und alle auf false gesetzt hat: allowWritingTools, allowImagePlayground, allowGenmoji, allowMailSummary, allowExternalIntelligenceIntegrations, allowAppleIntelligenceReport. Schema-strict-Validierung muss durchgehen. Keys, die nur auf iOS existieren (z.B. allowImageWand), dürfen für ein macOS-Profil weggelassen werden.",
      "files": [],
      "expectations": [
        "Output is a valid Apple plist parseable by plistlib",
        "Inner PayloadType is 'com.apple.applicationaccess'",
        "allowWritingTools is set and is the boolean False",
        "allowImagePlayground is set and is the boolean False",
        "allowGenmoji is set and is the boolean False",
        "allowMailSummary is set and is the boolean False",
        "allowExternalIntelligenceIntegrations is set and is the boolean False",
        "allowAppleIntelligenceReport is set and is the boolean False",
        "All AI-related allow* keys present in the payload have value False (not 0, not 'false' string)",
        "No iOS-only keys like allowImageWand are present (they would not validate strictly against schema for macOS)",
        "Strict-mode schema validation passes with zero errors"
      ]
    },
    {
      "id": 3,
      "prompt": "Klassenzimmer-Setup: ein einziges Profil, das Schüler-iPads mit unserem WLAN 'School-Net' (WPA2, Passwort 'schoolpass2026') verbindet und gleichzeitig App-Installation, In-App-Käufe und expliziten Inhalt verbietet, Kamera erlaubt. Identifier com.example.classroom.ipad.",
      "expected_output": "Ein .mobileconfig mit zwei Payloads im PayloadContent-Array: ein com.apple.wifi.managed mit den richtigen WLAN-Credentials, und ein com.apple.applicationaccess mit allowAppInstallation=false, allowInAppPurchases=false, allowExplicitContent=false, allowCamera=true. Beide Payloads haben unterschiedliche, deterministische UUIDs.",
      "files": ["assets/examples/classroom_ipad.json"],
      "expectations": [
        "Output is a valid Apple plist parseable by plistlib",
        "PayloadContent array has length 2",
        "Exactly one inner payload has PayloadType 'com.apple.wifi.managed'",
        "Exactly one inner payload has PayloadType 'com.apple.applicationaccess'",
        "Wi-Fi payload has SSID_STR equal to 'School-Net'",
        "Wi-Fi payload has EncryptionType 'WPA2'",
        "Restrictions payload has allowAppInstallation set to False",
        "Restrictions payload has allowCamera set to True",
        "Restrictions payload has allowInAppPurchases set to False",
        "Both inner payloads have distinct PayloadUUIDs (no collision)",
        "Both inner payloads have PayloadIdentifiers that are reverse-DNS strings derived from the top-level identifier",
        "Strict-mode schema validation passes with zero errors"
      ]
    },
    {
      "id": 4,
      "prompt": "Ich gebe dir absichtlich kaputten Input: PayloadType com.apple.wifi.managed mit EncryptionType='SUPERWPA' (gibts nicht) und AutoJoin='ja' (string statt boolean). Bau das mit strikter Validierung — was passiert?",
      "expected_output": "Strict-Mode-Build muss FAILEN, und zwar mit dem dokumentierten Validierungs-Exit-Code 2, nicht mit einem beliebigen Absturz. Die Fehlermeldung muss klar benennen, dass EncryptionType nicht im erlaubten Wertebereich liegt UND dass AutoJoin den falschen Typ hat. Es darf KEINE .mobileconfig-Datei erzeugt werden.",
      "files": [],
      "expectations": [
        "Build script exits with exit code 2 in strict mode (the documented validation failure, not an arbitrary crash)",
        "Error output is a validation report and contains no Python traceback",
        "Error output mentions 'EncryptionType' by name",
        "Error output mentions the invalid value 'SUPERWPA' or the allowed list (WEP/WPA/WPA2/WPA3/Any/None)",
        "Error output mentions 'AutoJoin' by name",
        "Error output indicates a type mismatch for AutoJoin (expected boolean)",
        "No .mobileconfig output file is created on the filesystem",
        "Errors are printed to stderr, not stdout"
      ]
    },
    {
      "id": 5,
      "prompt": "Frag mich nichts — ich will nur die Liste aller PayloadTypes sehen, die das Apple-Schema kennt. Aktueller Stand vom release-Branch.",
      "expected_output": "Aufruf von scripts/fetch_schema.py --list mit dem offline-Cache. Ausgabe ist eine sortierte Liste der PayloadTypes, jeweils mit unterstützten OS und Titel. Es müssen mindestens 50 Einträge sein (das Repo enthält ~127 Profil-Schemas), inklusive bekannter wie com.apple.wifi.managed, com.apple.vpn.managed, com.apple.applicationaccess.",
      "files": [],
      "expectations": [
        "fetch_schema.py --list executes and exits with code 0",
        "Output contains 'com.apple.wifi.managed'",
        "Output contains 'com.apple.applicationaccess'",
        "Output contains 'com.apple.vpn.managed'",
        "Output lists at least 50 distinct PayloadTypes",
        "Each line shows OS support markers like [iOS,macOS,...] or similar",
        "Output is sorted alphabetically by PayloadType"
      ]
    },
    {
      "id": 6,
      "prompt": "In meiner Spec steht auf oberster Ebene ein Key, den Apple gar nicht kennt: 'TotallyMadeUpKey'. Bau das mit strikter Validierung. Was passiert?",
      "expected_output": "Strict-Mode-Build muss failen (Exit-Code 2) und den erfundenen Key als Top-Level-Problem benennen, nicht als Payload-Problem. Es darf keine .mobileconfig entstehen. Ohne --validate-strict laeuft derselbe Input weiter durch, das ist die dokumentierte Bedeutung des Flags. Specs, deren Top-Level-Keys alle aus TopLevel.yaml stammen, muessen unveraendert bauen.",
      "files": ["assets/examples/wifi_guest.json"],
      "expectations": [
        "Strict build exits with code 2 for an invented top-level key",
        "Error output names the invented key 'TotallyMadeUpKey'",
        "Error output marks the finding as top-level, not as a payload finding",
        "No .mobileconfig output file is created in strict mode",
        "The same spec still builds without --validate-strict",
        "A spec whose top-level keys all come from TopLevel.yaml still builds in strict mode"
      ]
    },
    {
      "id": 7,
      "prompt": "Ich will ueber den macOS-Schluesselbund signieren, gebe aber eine Identitaet an, die es nicht gibt. Und in einem zweiten Versuch zeige ich mit --sign-cert auf eine PEM-Datei, die nicht existiert. Was macht das Werkzeug?",
      "expected_output": "Beide Faelle enden mit Exit-Code 2 und einer Meldung statt eines Tracebacks, und in beiden Faellen bleibt keine Datei zurueck. Besonders darf keine unsignierte Zwischendatei mit dem WLAN-Passwort im Klartext liegenbleiben. Auf macOS nennt die Meldung `security cms` und die angefragte Identitaet, auf anderen Plattformen sagt sie, dass der Schluesselbund-Weg macOS braucht und --sign-cert die Alternative ist. --sign-identity zusammen mit --sign-cert wird als Fehlbedienung abgelehnt. Und beim zweiten Bau auf denselben Pfad bleibt das dort liegende gueltige Profil unversehrt, wenn das Signieren scheitert.",
      "files": ["assets/examples/wifi_guest.json"],
      "expectations": [
        "Unknown keychain identity exits with code 2",
        "No output file is left behind after the failed keychain signing",
        "The failure is reported as a message, not as a Python traceback",
        "On macOS the message names security cms and the requested identity; on other platforms it names macOS and points to --sign-cert",
        "--sign-identity together with --sign-cert is rejected without a traceback",
        "A failed PEM signing leaves no file containing the cleartext password",
        "A failed PEM signing leaves no file at all in the output directory",
        "A failed signing run leaves an existing profile at the output path byte-for-byte unchanged"
      ]
    },
    {
      "id": 8,
      "prompt": "Ich will ein Chrome-Profil bauen. Apples Schema kennt com.google.Chrome nicht, ProfileManifests schon. Uebersetzt das Werkzeug so ein Manifest richtig in Apples Schema-Form?",
      "expected_output": "Der Normalisierer bildet pfm_domain auf den payloadtype ab, pfm_platforms auf supportedOS, pfm_type auf Apples valuetypes, pfm_require=always auf presence=required, pfm_range_list auf rangelist, pfm_range_min/max auf range, pfm_format auf format und verschachtelte pfm_subkeys auf subkeys. Bedienelemente von ProfileCreator (PFC_-Praefix, pfm_segments) und die CommonPayloadKeys fallen raus. Eine Spec gegen ein solches Manifest baut strikt durch, ein erfundener Key und ein Wert ausserhalb der Liste werden abgelehnt, und die Herkunft steht in der Meldung.",
      "files": [],
      "expectations": [
        "pfm_domain becomes the payloadtype and pfm_platforms becomes supportedOS",
        "pfm_type maps to Apple valuetypes and an unknown type becomes <any>",
        "Only pfm_require=always becomes presence=required",
        "pfm_range_list, pfm_range_min/max and pfm_format are carried over",
        "Nested pfm_subkeys and the item shape of an array survive the translation",
        "ProfileCreator control keys and the CommonPayloadKeys are dropped",
        "A spec built against the manifest passes --validate-strict",
        "An invented key and a value outside the range list are rejected with exit code 2",
        "The output names ProfileManifests as the source of the schema"
      ]
    },
    {
      "id": 9,
      "prompt": "Ich habe eine fertige .mobileconfig aus Jamf. Kann dein Werkzeug die pruefen, ohne dass ich sie neu baue?",
      "expected_output": "validate_mobileconfig.py liest die fertige Datei, prueft die Profil-Ebene gegen TopLevel.yaml und jeden Eintrag aus PayloadContent gegen sein Schema. Verletzungen des Schemas sind Fehler und enden mit Exit 2. Stellen, zu denen Apples Schema nichts sagt, sind Warnungen und enden mit Exit 1: unbekannte Keys, PayloadTypes ohne Schema, format-Regex, doppelte PayloadUUIDs. Mit --strict werden daraus Fehler. Jeder Befund nennt seinen Pfad, --format json gibt dieselben Befunde maschinenlesbar aus, und eine Datei, die kein Profil ist, endet mit einer Meldung statt mit einem Traceback.",
      "files": ["assets/examples/classroom_ipad.json"],
      "expectations": [
        "A profile this tool built validates with exit code 0",
        "An invented payload key is reported as a warning and exits 1 by default",
        "The same invented key becomes an error and exits 2 with --strict",
        "The finding names the invented key and the path PayloadContent[0]",
        "A value outside the schema's range list is an error and exits 2 without --strict",
        "An invented top-level key is reported under top-level, not under a payload",
        "A payload type without a schema is reported as unchecked, not as an error",
        "--format json emits parseable JSON with level, path and exit code",
        "A file that is not a property list exits 2 without a Python traceback"
      ]
    },
    {
      "id": 10,
      "prompt": "Ich will ein Zertifikat in mein Profil packen und meine Spec ist JSON. Bisher hiess es, dafuer muss ich YAML schreiben. Geht das jetzt?",
      "expected_output": "Der Builder loest die Marker {\"__base64__\": \"...\"} und {\"__file__\": \"pfad\"} vor der Validierung im ganzen Spec-Baum in Bytes auf, in beliebiger Tiefe und auch in Listen. Ein relativer Pfad zaehlt vom Verzeichnis der Spec aus. Die Bytes im Profil sind Byte fuer Byte das Dekodierte beziehungsweise der Dateiinhalt. Kaputtes Base64, ein nicht lesbarer Pfad und ein Marker neben einem anderen Key enden mit Exit 2, mit Meldung statt Traceback und ohne Ausgabedatei. Ein nackter Base64-String bleibt eine Zeichenkette und faellt weiter als expected <data>, got str durch.",
      "files": [],
      "expectations": [
        "A JSON spec with a __base64__ marker builds with --validate-strict",
        "The <data> value in the profile is byte-for-byte the decoded input",
        "A __file__ marker reads the file next to the spec and yields the same bytes",
        "Invalid base64 exits 2 with a message and no traceback",
        "A __file__ path that does not exist exits 2 and names the path",
        "A marker next to another key is rejected as ambiguous, exit 2",
        "No output file is left behind by any of the rejected specs",
        "A plain base64 string is still a string and still fails as expected <data>",
        "Markers are resolved at any depth, inside dictionaries and inside arrays"
      ]
    }
  ]
}
